- alert: MAASIPExhaustionWarning
  expr: count(maas_net_subnet_ip_count{status="available"}) by cidr < 5
  for: 1m
  labels:
    severity: warn
  annotations:
    summary: {{ $labels.subnet }} is getting low on available IPs
    description: The subnet is close to exhausting all IPs
